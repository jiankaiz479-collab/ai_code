# --- 核心 Web 框架 ---
Django>=5.0,<7.0
python-dotenv>=1.0

# --- AI 與影像處理 ---
google-genai>=1.0
rembg[cpu]>=2.0
pillow>=10.0
opencv-python-headless>=4.8
numpy>=1.26,<3.0
scipy>=1.11

# --- DensePose / Detectron2 核心依賴 (從 Dockerfile 移入) ---
cython
pycocotools>=2.0
termcolor
yacs
tabulate
cloudpickle
matplotlib
tqdm
omegaconf
fvcore
iopath
av>=12.0

# --- 網路工具 ---
requests>=2.31