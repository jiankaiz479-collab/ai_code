# 固定在 5.0.x 系列，確保 Django 語法不會因為跳到 6.0 而改變
Django==5.0.3
# 環境變數管理
python-dotenv==1.0.1

# --- AI 與影像處理 ---
# Google Gemini API 最新穩定版
google-genai==1.0.0
# 去背核心 (固定使用 CPU 版)
rembg[cpu]==2.0.53
# 影像處理 (PIL) 確保與 rembg 相容
pillow==10.2.0
# OpenCV 無介面版 (固定 4.8 系列，非常穩定)
opencv-python-headless==4.8.1.78
# Numpy 嚴格鎖定在 1.26.4，避免 2.0 造成的相容性大災難
numpy==1.26.4
# 科學計算
scipy==1.11.4

# --- 網路工具 ---
# 確保 API 請求穩定
requests==2.31.0

# --- 影像格式支援 ---
# HEIC/HEIF 支援 (iPhone 預設 "高效率" 格式)
pillow-heif>=0.16.0

# --- Human Parsing (SCHP) 支援 ---
onnxruntime==1.17.1

# --- Human Parsing (SCHP) 支援 ---
onnxruntime==1.17.1
